// Intentionally empty — marketing pages use ISR and respond from cache quickly.
// A visible skeleton would flash unnecessarily and feel like added latency.
export default function MarketingLoading() {
  return null;
}
